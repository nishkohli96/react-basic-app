import React from 'react';
import { useTranslation } from 'react-i18next';

const LangChange = () => {
    // const { t, i18n } = useTranslation();
    // console.log('tyreg ',useTranslation());
    return (
            // <h1>{t('welcomeText')}</h1>
            <p>de</p>
    );
};

export default LangChange;
